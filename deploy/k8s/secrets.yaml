# TEMPLATE — replace placeholder values before applying.
# Do NOT commit real secrets. Use a secrets manager (AWS Secrets Manager,
# External Secrets Operator, Sealed Secrets, etc.) in production.
#
# Apply with:
#   kubectl apply -f deploy/k8s/secrets.yaml -n pr-reviewer
---
apiVersion: v1
kind: Secret
metadata:
  name: pr-reviewer-secrets
  namespace: pr-reviewer
type: Opaque
stringData:
  ANTHROPIC_API_KEY: "REPLACE_ME"
  GITHUB_TOKEN: "REPLACE_ME"
  # Optional — defaults to claude-sonnet-4-6 if omitted
  PR_REVIEWER_MODEL: ""
---
apiVersion: v1
kind: Secret
metadata:
  name: webhook-secrets
  namespace: pr-reviewer
type: Opaque
stringData:
  WEBHOOK_SECRET: "REPLACE_ME"
